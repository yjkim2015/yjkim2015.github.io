---
title: "MSA 실전 가이드 — 모놀리스에서 마이크로서비스로"
categories:
- ARCHITECTURE
toc: true
toc_sticky: true
toc_label: 목차
---

## 실생활 비유: 백화점 vs 전문점 거리

**모놀리식 아키텍처**는 백화점입니다. 의류, 식품, 가전 모두 한 건물 안에 있어서 관리가 쉽지만, 식품관에 화재가 나면 전체 백화점이 닫힙니다.

**MSA**는 명동 같은 전문점 거리입니다. 각 가게(서비스)가 독립적으로 운영됩니다. 옷 가게가 휴업해도 밥은 먹을 수 있습니다. 장사가 잘 되는 가게만 직원을 더 뽑을 수 있습니다.

---

## 1. 모놀리스의 문제점

```mermaid
graph TD
    subgraph 모놀리식 애플리케이션
        UserModule[사용자 모듈]
        OrderModule[주문 모듈]
        PaymentModule[결제 모듈]
        DeliveryModule[배송 모듈]
        NotifyModule[알림 모듈]
        SearchModule[검색 모듈]
    end

    AllModules[하나의 배포 단위] --> UserModule
    AllModules --> OrderModule
    AllModules --> PaymentModule
    AllModules --> DeliveryModule

    P1[문제1: 결제 모듈 버그 → 전체 서비스 다운]
    P2[문제2: 주문 트래픽 급증 → 전체 서버 증설 필요]
    P3[문제3: 특정 모듈 기술 변경 → 전체 재배포]
    P4[문제4: 코드베이스 비대화 → 빌드 20분]
```

---

## 2. MSA 핵심 원칙

```mermaid
graph TD
    MSA_Principles[MSA 핵심 원칙]
    MSA_Principles --> P1[단일 책임<br/>하나의 서비스 = 하나의 비즈니스 기능]
    MSA_Principles --> P2[독립 배포<br/>다른 서비스 영향 없이 배포]
    MSA_Principles --> P3[데이터 격리<br/>서비스별 독립 DB]
    MSA_Principles --> P4[장애 격리<br/>하나의 서비스 장애가 전파 안됨]
    MSA_Principles --> P5[기술 다양성<br/>서비스별 다른 언어/DB 허용]
```

---

## 3. 모놀리스 → MSA 전환 전략

### Strangler Fig Pattern (교살자 무화과 패턴)

```mermaid
graph TD
    subgraph 단계1: 초기
        Mono1[모놀리스 전체]
    end

    subgraph 단계2: 점진적 분리
        Mono2[모놀리스 메인]
        UserSvc[사용자 서비스 분리됨]
    end

    subgraph 단계3: 더 분리
        Mono3[모놀리스 코어]
        UserSvc2[사용자 서비스]
        PaySvc[결제 서비스 분리됨]
        OrderSvc[주문 서비스 분리됨]
    end

    subgraph 최종: 완전 MSA
        US[사용자]
        OS[주문]
        PS[결제]
        DS[배송]
        NS[알림]
    end

    Mono1 --> Mono2
    Mono2 --> Mono3
    Mono3 --> US
```

### 서비스 분리 기준 (DDD — Domain Driven Design)

```mermaid
graph TD
    DDD[도메인 주도 설계]
    DDD --> BC[바운디드 컨텍스트<br/>Bounded Context]
    BC --> User[사용자 도메인<br/>회원가입, 인증, 프로필]
    BC --> Order[주문 도메인<br/>장바구니, 주문, 취소]
    BC --> Payment[결제 도메인<br/>결제, 환불, 정산]
    BC --> Delivery[배송 도메인<br/>배송 추적, 배송사 연동]
    BC --> Catalog[상품 도메인<br/>상품 등록, 카테고리, 검색]
```

---

## 4. 전체 MSA 아키텍처

```mermaid
graph TD
    Client[모바일/웹 클라이언트]
    Client --> AG[API Gateway<br/>인증/인가/라우팅/Rate Limit]

    AG --> US[사용자 서비스<br/>Java Spring Boot]
    AG --> OS[주문 서비스<br/>Java Spring Boot]
    AG --> PS[결제 서비스<br/>Java Spring Boot]
    AG --> DS[배송 서비스<br/>Python FastAPI]
    AG --> CS[카탈로그 서비스<br/>Node.js]
    AG --> SS[검색 서비스<br/>Elasticsearch]

    OS -->|이벤트 발행| Kafka[Kafka 메시지 버스]
    Kafka --> PS
    Kafka --> DS
    Kafka --> NS[알림 서비스]

    US --> UserDB[(사용자 DB<br/>MySQL)]
    OS --> OrderDB[(주문 DB<br/>MySQL)]
    PS --> PayDB[(결제 DB<br/>PostgreSQL)]
    DS --> DeliveryDB[(배송 DB<br/>MongoDB)]
    CS --> CatalogDB[(상품 DB<br/>MySQL)]
    SS --> ESCluster[(Elasticsearch)]

    subgraph 인프라
        SD[서비스 디스커버리<br/>Consul/Eureka]
        CB[Circuit Breaker<br/>Resilience4j]
        DT[분산 추적<br/>Zipkin/Jaeger]
        Config[설정 서버<br/>Spring Cloud Config]
    end
```

---

## 5. API Gateway

```mermaid
graph TD
    Client[클라이언트] --> AG[API Gateway]

    AG --> Auth[인증/인가<br/>JWT 검증]
    AG --> RL[Rate Limiting<br/>사용자별 제한]
    AG --> LB[로드밸런싱]
    AG --> Trans[프로토콜 변환<br/>REST ↔ gRPC]
    AG --> Agg[응답 집계<br/>여러 서비스 응답 합치기]
    AG --> Log[요청 로깅]

    AG --> US[사용자 서비스]
    AG --> OS[주문 서비스]
    AG --> PS[결제 서비스]
```

**Spring Cloud Gateway 설정:**
```yaml
# application.yml
spring:
  cloud:
    gateway:
      routes:
        - id: user-service
          uri: lb://user-service        # 로드밸런서 자동 연결
          predicates:
            - Path=/api/users/**
          filters:
            - StripPrefix=2             # /api/users → /users
            - name: RequestRateLimiter
              args:
                redis-rate-limiter.replenishRate: 100
                redis-rate-limiter.burstCapacity: 200

        - id: order-service
          uri: lb://order-service
          predicates:
            - Path=/api/orders/**
          filters:
            - name: CircuitBreaker
              args:
                name: orderCircuitBreaker
                fallbackUri: forward:/fallback/orders

        - id: auth-route
          uri: lb://auth-service
          predicates:
            - Path=/api/auth/**
          filters:
            - RemoveRequestHeader=Cookie

      globalcors:
        corsConfigurations:
          '[/**]':
            allowedOrigins: "https://myapp.com"
            allowedMethods: "*"
            allowedHeaders: "*"
```

---

## 6. 서비스 간 통신

### 동기 통신: REST vs gRPC

```mermaid
graph LR
    subgraph REST
        R_Call[HTTP/JSON 호출]
        R_Call -->|느림 텍스트 파싱| R_Response[응답]
    end

    subgraph gRPC
        G_Call[HTTP/2 + Protobuf 호출]
        G_Call -->|빠름 바이너리| G_Response[응답]
    end
```

**gRPC 서비스 정의:**
```protobuf
// order.proto
syntax = "proto3";

service OrderService {
  rpc CreateOrder (CreateOrderRequest) returns (OrderResponse);
  rpc GetOrder (GetOrderRequest) returns (OrderResponse);
  rpc ListOrders (ListOrdersRequest) returns (stream OrderResponse);
}

message CreateOrderRequest {
  string user_id = 1;
  repeated OrderItem items = 2;
  string delivery_address = 3;
}

message OrderResponse {
  string order_id = 1;
  string status = 2;
  int64 total_amount = 3;
  string created_at = 4;
}
```

### 비동기 통신: Kafka 이벤트

```mermaid
sequenceDiagram
    participant OS as 주문 서비스
    participant Kafka as Kafka
    participant PS as 결제 서비스
    participant DS as 배송 서비스
    participant NS as 알림 서비스

    OS->>OS: 주문 생성
    OS->>Kafka: OrderCreated 이벤트 발행
    OS-->>Client: 즉시 응답 (비동기!)

    par 병렬 처리
        Kafka->>PS: OrderCreated 소비
        PS->>PS: 결제 처리
        PS->>Kafka: PaymentCompleted 발행
    and
        Kafka->>NS: OrderCreated 소비
        NS->>NS: 주문 접수 알림 발송
    end

    Kafka->>DS: PaymentCompleted 소비
    DS->>DS: 배송 준비 시작
    DS->>Kafka: DeliveryStarted 발행

    Kafka->>NS: DeliveryStarted 소비
    NS->>NS: 배송 시작 알림 발송
```

| 통신 방식 | 프로토콜 | 장점 | 단점 | 사용 예 |
|---------|---------|------|------|---------|
| REST | HTTP/JSON | 단순, 범용 | 느림 | 외부 API |
| gRPC | HTTP2/Protobuf | 빠름, 타입 안전 | 설정 복잡 | 내부 서비스 |
| Kafka | TCP | 비동기, 내구성 | 복잡도 높음 | 이벤트 드리븐 |

---

## 7. 서비스 디스커버리

### 왜 필요한가?

```mermaid
graph TD
    Problem[문제: 서비스 IP가 동적으로 변함<br/>Auto Scaling, 재배포]

    Static[❌ 정적 IP 설정<br/>service-b: 10.0.1.5:8080<br/>재배포 시 IP 변경되면?]

    Dynamic[✅ 서비스 디스커버리<br/>service-b: 어디에 있어?<br/>→ Consul/Eureka에게 물어봄]
```

**Eureka 서비스 등록:**
```java
// OrderServiceApplication.java
@SpringBootApplication
@EnableEurekaClient
public class OrderServiceApplication {
    public static void main(String[] args) {
        SpringApplication.run(OrderServiceApplication.class, args);
    }
}
```

```yaml
# application.yml
spring:
  application:
    name: order-service

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka/
  instance:
    prefer-ip-address: true
    lease-renewal-interval-in-seconds: 10
    lease-expiration-duration-in-seconds: 30
```

**서비스 호출 (클라이언트 사이드 로드밸런싱):**
```java
@Service
public class OrderService {

    @Autowired
    private WebClient.Builder webClientBuilder;

    public PaymentResponse processPayment(PaymentRequest request) {
        // "payment-service" → Eureka에서 실제 IP:Port 자동 조회
        return webClientBuilder.build()
            .post()
            .uri("http://payment-service/api/payments")
            .bodyValue(request)
            .retrieve()
            .bodyToMono(PaymentResponse.class)
            .block();
    }
}
```

---

## 8. Circuit Breaker (서킷 브레이커)

> 비유: 집의 두꺼비집(차단기). 과전류가 흐르면 자동으로 차단하여 더 큰 피해를 막습니다.

```mermaid
stateDiagram-v2
    [*] --> Closed: 초기 상태 (정상)

    Closed --> Open: 연속 실패 5회 초과
    Open --> HalfOpen: 30초 대기 후
    HalfOpen --> Closed: 테스트 성공
    HalfOpen --> Open: 테스트 실패

    state Closed {
        [*] --> 요청통과
        요청통과 --> 실패카운터증가
    }

    state Open {
        [*] --> 즉시실패반환
        즉시실패반환 --> Fallback응답
    }

    state HalfOpen {
        [*] --> 제한된요청허용
    }
```

**Resilience4j 구현:**
```java
@Service
public class PaymentService {

    private final CircuitBreaker circuitBreaker;
    private final Retry retry;

    public PaymentService(CircuitBreakerRegistry registry) {
        // CircuitBreaker 설정
        CircuitBreakerConfig config = CircuitBreakerConfig.custom()
            .failureRateThreshold(50)           // 50% 실패율에서 OPEN
            .slowCallRateThreshold(100)         // 느린 호출 100%면 OPEN
            .slowCallDurationThreshold(Duration.ofSeconds(2))
            .waitDurationInOpenState(Duration.ofSeconds(30))
            .permittedNumberOfCallsInHalfOpenState(5)
            .slidingWindowSize(10)
            .build();

        this.circuitBreaker = registry.circuitBreaker("paymentService", config);

        // Retry 설정
        RetryConfig retryConfig = RetryConfig.custom()
            .maxAttempts(3)
            .waitDuration(Duration.ofMillis(500))
            .retryOnException(e -> e instanceof NetworkException)
            .build();

        this.retry = Retry.of("paymentRetry", retryConfig);
    }

    public PaymentResult charge(PaymentRequest request) {
        // CircuitBreaker + Retry 체인
        Supplier<PaymentResult> decoratedSupplier = CircuitBreaker
            .decorateSupplier(circuitBreaker,
                Retry.decorateSupplier(retry,
                    () -> paymentApiClient.charge(request)
                )
            );

        return Try.ofSupplier(decoratedSupplier)
            .recover(throwable -> fallbackPayment(request))  // Fallback
            .get();
    }

    private PaymentResult fallbackPayment(PaymentRequest request) {
        // 결제 서비스 장애 시 → 임시 처리 (나중에 재시도)
        log.warn("Payment service unavailable, queuing for retry");
        paymentRetryQueue.add(request);
        return PaymentResult.pending(request.getOrderId());
    }
}
```

---

## 9. Saga 패턴 (분산 트랜잭션)

MSA에서는 하나의 비즈니스 트랜잭션이 여러 서비스에 걸쳐 있습니다. 분산 트랜잭션을 어떻게 관리할까요?

```mermaid
sequenceDiagram
    participant OS as 주문 서비스
    participant PS as 결제 서비스
    participant IS as 재고 서비스
    participant DS as 배송 서비스

    Note over OS,DS: 정상 흐름
    OS->>OS: 주문 생성 (PENDING)
    OS->>PS: 결제 요청
    PS->>PS: 결제 처리
    PS-->>OS: 결제 완료
    OS->>IS: 재고 차감
    IS-->>OS: 재고 차감 완료
    OS->>DS: 배송 요청
    DS-->>OS: 배송 시작
    OS->>OS: 주문 완료

    Note over OS,DS: 실패 흐름 (보상 트랜잭션)
    OS->>OS: 주문 생성 (PENDING)
    OS->>PS: 결제 요청
    PS-->>OS: 결제 완료
    OS->>IS: 재고 차감
    IS-->>OS: ❌ 재고 부족!
    OS->>PS: 결제 취소 (보상 트랜잭션!)
    PS-->>OS: 취소 완료
    OS->>OS: 주문 취소
```

---

## 10. 분산 추적 (Distributed Tracing)

```mermaid
graph LR
    Request[사용자 요청] --> AG[API Gateway<br/>TraceID: abc123]
    AG --> OS[주문 서비스<br/>SpanID: span1]
    OS --> PS[결제 서비스<br/>SpanID: span2]
    OS --> IS[재고 서비스<br/>SpanID: span3]
    OS --> DS[배송 서비스<br/>SpanID: span4]

    AG --> Zipkin[Zipkin/Jaeger<br/>추적 수집 서버]
    OS --> Zipkin
    PS --> Zipkin
    IS --> Zipkin
```

**Spring Sleuth + Zipkin 설정:**
```yaml
# application.yml
spring:
  sleuth:
    sampler:
      probability: 1.0  # 100% 샘플링 (프로덕션은 0.1)
  zipkin:
    base-url: http://zipkin-server:9411
```

```java
@RestController
public class OrderController {

    private static final Logger log = LoggerFactory.getLogger(OrderController.class);

    @PostMapping("/orders")
    public ResponseEntity<Order> createOrder(@RequestBody OrderRequest request) {
        // Trace ID가 자동으로 로그에 포함됨
        // 2024-01-01 [abc123,span1] Creating order for user: 1001
        log.info("Creating order for user: {}", request.getUserId());

        Order order = orderService.createOrder(request);
        // 다른 서비스 호출 시 Trace ID 자동 전파 (HTTP 헤더)
        return ResponseEntity.ok(order);
    }
}
```

---

## 11. 카나리 배포 (Canary Deployment)

```mermaid
graph TD
    AG[API Gateway] --> Old[구버전 서비스<br/>v1.0 - 90% 트래픽]
    AG --> New[신버전 서비스<br/>v2.0 - 10% 트래픽]

    New --> Monitor[모니터링<br/>에러율, 응답시간]
    Monitor -->|정상| Rollout[점진적 증가<br/>10% → 30% → 50% → 100%]
    Monitor -->|문제 감지| Rollback[즉시 롤백<br/>100% v1.0으로]
```

---

## 12. 극한 시나리오: 카카오 주문하기 MSA

카카오 선물하기 같은 서비스를 MSA로 설계할 때:

```mermaid
graph TD
    App[카카오톡 앱] --> AG[API Gateway<br/>Kong/Spring Cloud]

    AG --> AuthSvc[인증 서비스<br/>JWT + OAuth2]
    AG --> UserSvc[사용자 서비스<br/>프로필, 주소]
    AG --> CatalogSvc[상품 카탈로그<br/>상품 검색/조회]
    AG --> CartSvc[장바구니 서비스<br/>Redis 기반]
    AG --> OrderSvc[주문 서비스<br/>핵심 도메인]
    AG --> PaySvc[결제 서비스<br/>PCI DSS 준수]
    AG --> DeliverySvc[배송 서비스<br/>외부 API 연동]
    AG --> NotifySvc[알림 서비스<br/>카카오톡 메시지]
    AG --> ReviewSvc[리뷰 서비스]

    subgraph 이벤트 버스
        Kafka[Kafka Cluster]
        OrderSvc -->|OrderCreated| Kafka
        Kafka --> PaySvc
        Kafka --> NotifySvc
        Kafka --> AnalyticsSvc[분석 서비스]
    end

    subgraph 데이터 계층
        OrderSvc --> OrderDB[(주문 DB MySQL)]
        PaySvc --> PayDB[(결제 DB PostgreSQL)]
        CatalogSvc --> CatalogDB[(상품 DB MySQL)]
        CatalogSvc --> ESSearch[(Elasticsearch)]
        CartSvc --> CartRedis[(Redis)]
        UserSvc --> UserDB[(사용자 DB MySQL)]
    end
```

---

## MSA 도입 판단 기준

```mermaid
graph TD
    Q1{팀 규모 > 20명?}
    Q1 -->|No| Mono[모놀리스 유지<br/>MSA 오버엔지니어링]
    Q1 -->|Yes| Q2{독립 배포 필요?}
    Q2 -->|No| Mono
    Q2 -->|Yes| Q3{서비스별 확장 필요?}
    Q3 -->|No| Mono
    Q3 -->|Yes| MSA_Candidate[MSA 검토 대상]
    MSA_Candidate --> Start[작은 서비스부터<br/>Strangler Fig Pattern]
```

| 조건 | 모놀리스 | MSA |
|------|---------|-----|
| 팀 규모 | < 10명 | > 20명 |
| 서비스 규모 | 초기 스타트업 | 성숙한 서비스 |
| 배포 빈도 | 주 1회 | 하루 수십 회 |
| 독립 확장 필요 | 없음 | 있음 |
| 기술 다양성 | 필요 없음 | 필요 |

> **결론**: MSA는 문제를 해결하지만 새로운 복잡도를 만듭니다. "필요할 때" 도입하세요. 모놀리스가 나쁜 것이 아닙니다.
