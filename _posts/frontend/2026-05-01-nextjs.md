---
title: "Next.js"
categories: FRONTEND
tags: [Next.js, SSR, SSG, ISR, App Router, Server Components, 성능 최적화]
toc: true
toc_sticky: true
toc_label: 목차
---

> **한 줄 요약**: Next.js는 React 기반 풀스택 프레임워크로, SSG·SSR·ISR·CSR 렌더링 전략을 상황에 맞게 조합하고 App Router의 서버 컴포넌트로 성능과 SEO를 동시에 잡을 수 있습니다.

---

## 비유로 이해하기

음식 제공 방식에 비유하면 렌더링 전략을 쉽게 이해할 수 있습니다. **CSR(Client-Side Rendering)**은 일반 식당입니다. 손님이 오면 그때서야 요리를 시작하고, 다 완성되기 전까지 손님은 빈 접시를 봐야 합니다. **SSG(Static Site Generation)**는 뷔페입니다. 미리 음식을 만들어 진열해두고, 손님이 오면 즉시 제공합니다. 메뉴가 바뀌려면 다음 영업까지 기다려야 합니다. **ISR(Incremental Static Regeneration)**은 정기배송 도시락입니다. 미리 만들어두되, 주기적으로 새로 만들어 교체합니다. **SSR(Server-Side Rendering)**은 개인 맞춤 즉석 조리입니다. 손님 정보에 맞게 그 자리에서 만들어 제공합니다.

Next.js는 이 네 가지 방식을 **하나의 프레임워크 안에서 페이지별로 선택**할 수 있게 해줍니다. 블로그 게시글은 SSG로, 개인화된 대시보드는 SSR로, 실시간 채팅은 CSR로 구현하면 됩니다.

App Router(Next.js 13+)는 여기서 한 단계 더 나아가, 서버에서만 실행되는 Server Component와 브라우저에서 실행되는 Client Component를 세밀하게 나누어 JavaScript 번들 크기를 최소화합니다.

---

## 1. 렌더링 전략 비교

<div class="mermaid">
flowchart TD
    subgraph "1️⃣ CSR (Client-Side Rendering)"
        C1["브라우저 요청"] --> C2["빈 HTML 반환\n(빠른 TTFB)"]
        C2 --> C3["JS 번들 다운로드\n(느린 FCP)"]
        C3 --> C4["React가 DOM 생성\n(느린 TTI)"]
    end

    subgraph "2️⃣ SSG (Static Site Generation)"
        S1["빌드 시 HTML 생성"] --> S2["CDN 배포"]
        S2 --> S3["브라우저 요청"]
        S3 --> S4["완성된 HTML 즉시 반환\n(가장 빠름)"]
    end

    subgraph "3️⃣ ISR (Incremental Static Regeneration)"
        I1["첫 요청: 캐시된 HTML"] --> I2["revalidate 시간 경과"]
        I2 --> I3["백그라운드에서 재생성"]
        I3 --> I1
    end

    subgraph "4️⃣ SSR (Server-Side Rendering)"
        R1["브라우저 요청"] --> R2["서버에서 데이터 조회\n+ HTML 생성"]
        R2 --> R3["완성된 HTML 전송\n(느린 TTFB)"]
        R3 --> R4["Hydration\n(인터랙티브)"]
    end

    style C4 fill:#e74c3c,color:#fff
    style S4 fill:#2ecc71,color:#fff
    style I1 fill:#f39c12,color:#fff
    style R4 fill:#3498db,color:#fff
</div>

| 전략 | 빌드 시 생성 | 요청 시 생성 | SEO | 실시간 데이터 | 사용 예 |
|------|------------|------------|-----|-------------|---------|
| CSR | X | O (클라이언트) | 나쁨 | 가능 | 대시보드, 관리자 페이지 |
| SSG | O | X | 좋음 | 불가 | 블로그, 문서, 마케팅 |
| ISR | O | X (주기 갱신) | 좋음 | 준실시간 | 뉴스, 상품 목록 |
| SSR | X | O (서버) | 좋음 | 가능 | 개인화 피드, 실시간 |

#### 왜 이게 중요한가?

렌더링 전략 선택은 사용자 경험과 SEO에 직결됩니다. 블로그 글을 CSR로 구현하면 검색 엔진이 빈 HTML만 보게 되어 SEO가 망가집니다. 반대로 개인화된 대시보드를 SSR로 구현하면 서버 부하가 크고 응답이 느려집니다. 각 전략의 트레이드오프를 이해하고 페이지 성격에 맞게 선택해야 합니다.

---

## 2. App Router 파일 구조

<div class="mermaid">
flowchart TD
    subgraph "app/ 디렉토리 규약"
        ROOT["app/"]
        ROOT --> LAYOUT["layout.tsx\n모든 페이지 공유 레이아웃\nHTML, body 태그"]
        ROOT --> PAGE["page.tsx\n/ 루트 페이지"]
        ROOT --> LOADING["loading.tsx\nSuspense 자동 적용\n로딩 UI"]
        ROOT --> ERROR["error.tsx\nError Boundary 자동\n에러 UI"]
        ROOT --> NOT_FOUND["not-found.tsx\n404 UI"]
        ROOT --> PRODUCTS["products/"]
        PRODUCTS --> PROD_LAYOUT["layout.tsx\n/products 레이아웃"]
        PRODUCTS --> PROD_PAGE["page.tsx\n/products 페이지"]
        PRODUCTS --> DYN["[id]/page.tsx\n/products/123 동적 라우트"]
        ROOT --> API["api/orders/route.ts\n/api/orders API Route"]
        ROOT --> AUTH["(auth)/\nURL에 포함 안 됨\n레이아웃 그룹"]
    end

    style LAYOUT fill:#3498db,color:#fff
    style PAGE fill:#2ecc71,color:#fff
    style ERROR fill:#e74c3c,color:#fff
    style LOADING fill:#f39c12,color:#fff
</div>

```
app/
├── layout.tsx          # 루트 레이아웃 (모든 페이지 공유)
├── page.tsx            # / 루트 페이지
├── loading.tsx         # 로딩 UI (Suspense 자동 적용)
├── error.tsx           # 에러 UI (Error Boundary 자동)
├── not-found.tsx       # 404 UI
├── products/
│   ├── layout.tsx      # /products 레이아웃
│   ├── page.tsx        # /products 페이지
│   └── [id]/
│       └── page.tsx    # /products/123 동적 페이지
├── api/
│   └── orders/
│       └── route.ts    # /api/orders API Route
└── (auth)/             # 괄호: URL에 포함 안 됨 (레이아웃 그룹)
    ├── login/
    └── register/
```

```tsx
// app/products/page.tsx - 기본 Server Component 페이지
export default async function ProductsPage() {
    // 서버에서 직접 실행 → API key 노출 없음, 번들 크기 0
    const products = await fetch('https://api.example.com/products', {
        next: { revalidate: 60 }  // ISR: 60초마다 재검증
    }).then(r => r.json());

    return (
        <div>
            <h1>상품 목록</h1>
            {products.map(product => (
                <ProductCard key={product.id} product={product} />
            ))}
        </div>
    );
}
```

---

## 3. Server Components vs Client Components

<div class="mermaid">
flowchart TD
    subgraph "Server Component (기본)"
        SC["서버에서만 실행\n브라우저에 JS 전송 안 함\nuseState, useEffect 사용 불가\nDB 직접 접근 가능\nAPI key 안전"]
    end

    subgraph "Client Component ('use client')"
        CC["브라우저에서 실행\nJS 번들에 포함\nuseState, useEffect 사용 가능\n이벤트 핸들러 가능\nwindow, document 접근 가능"]
    end

    subgraph "조합 패턴"
        SP["Server Page\n(데이터 페칭)"]
        CP["Client Button\n(인터랙션)"]
    end

    SC -->|"Client Component\n자식으로 포함 가능"| CC
    SP -->|"props로 데이터 전달"| CP

    style SC fill:#3498db,color:#fff
    style CC fill:#f39c12,color:#fff
</div>

```tsx
// ① Server Component: DB 직접 접근, 번들 크기 0
// app/products/[id]/page.tsx
async function getProduct(id: string) {
    const product = await db.product.findUnique({ where: { id } });
    return product;
}

export default async function ProductPage({ params }: { params: { id: string } }) {
    const product = await getProduct(params.id);

    return (
        <div>
            <h1>{product.name}</h1>
            <p>{product.description}</p>
            <p>₩{product.price.toLocaleString()}</p>
            {/* Client Component를 자식으로 포함 */}
            <AddToCartButton productId={product.id} />
        </div>
    );
}

// SEO 메타데이터 - 서버에서 동적 생성
export async function generateMetadata({ params }) {
    const product = await getProduct(params.id);
    return {
        title: product.name,
        description: product.description,
        openGraph: { images: [product.imageUrl] }
    };
}

// ② Client Component: 상호작용이 필요한 부분만 분리
// components/AddToCartButton.tsx
'use client'

import { useState } from 'react';
import { useCartStore } from '@/store/cart';

export function AddToCartButton({ productId }: { productId: string }) {
    const [added, setAdded] = useState(false);
    const addToCart = useCartStore(state => state.addItem);

    const handleClick = async () => {
        await addToCart(productId);
        setAdded(true);
        setTimeout(() => setAdded(false), 2000);
    };

    return (
        <button onClick={handleClick} disabled={added}>
            {added ? '담겼습니다!' : '장바구니 담기'}
        </button>
    );
}
```

#### Server Component vs Client Component 선택 기준

| 상황 | 선택 | 이유 |
|------|------|------|
| 데이터 페칭 (DB, API) | Server | JS 번들에 포함 안 됨 |
| useState, useEffect 필요 | Client | Hook은 브라우저에서만 동작 |
| 이벤트 핸들러 (onClick 등) | Client | 브라우저 이벤트 |
| window, document 접근 | Client | 브라우저 API |
| 무거운 라이브러리 (그래프 등) | 가능하면 Server | 번들 크기 절감 |
| 민감한 API key 사용 | Server | 클라이언트 노출 방지 |

---

## 4. API Routes

```typescript
// app/api/orders/route.ts
import { NextRequest, NextResponse } from 'next/server';

export async function GET(request: NextRequest) {
    const searchParams = request.nextUrl.searchParams;
    const userId = searchParams.get('userId');

    if (!userId) {
        return NextResponse.json({ error: 'userId required' }, { status: 400 });
    }

    const orders = await db.order.findMany({
        where: { customerId: userId },
        include: { items: true }
    });

    return NextResponse.json(orders);
}

export async function POST(request: NextRequest) {
    const body = await request.json();

    // 서버 세션으로 인증 확인 (토큰 클라이언트 노출 없음)
    const session = await getServerSession();
    if (!session) {
        return NextResponse.json({ error: 'Unauthorized' }, { status: 401 });
    }

    const order = await db.order.create({
        data: { ...body, customerId: session.user.id }
    });
    return NextResponse.json(order, { status: 201 });
}

// app/api/orders/[id]/route.ts - 동적 세그먼트
export async function GET(
    request: NextRequest,
    { params }: { params: { id: string } }
) {
    const order = await db.order.findUnique({ where: { id: params.id } });
    if (!order) {
        return NextResponse.json({ error: 'Not Found' }, { status: 404 });
    }
    return NextResponse.json(order);
}

export async function PATCH(
    request: NextRequest,
    { params }: { params: { id: string } }
) {
    const body = await request.json();
    const updated = await db.order.update({
        where: { id: params.id },
        data: body
    });
    return NextResponse.json(updated);
}
```

---

## 5. 미들웨어 (Middleware)

미들웨어는 요청이 페이지에 도달하기 **전에 실행**됩니다. Edge Runtime에서 실행되어 매우 빠릅니다.

<div class="mermaid">
sequenceDiagram
    participant Browser as 브라우저
    participant Edge as Edge Runtime
    participant MW as middleware.ts
    participant Page as 페이지/API

    Browser->>Edge: GET /dashboard
    Edge->>MW: 미들웨어 실행
    MW->>MW: 쿠키에서 auth-token 확인
    alt 토큰 없음
        MW-->>Browser: 302 Redirect /login?redirect=/dashboard
    else 토큰 있음
        MW->>Page: 요청 통과 + 헤더 추가
        Page-->>Browser: 200 페이지 응답
    end
</div>

```typescript
// middleware.ts (프로젝트 루트에 위치)
import { NextResponse } from 'next/server';
import type { NextRequest } from 'next/server';

export function middleware(request: NextRequest) {
    const token = request.cookies.get('auth-token')?.value;
    const { pathname } = request.nextUrl;

    // 보호된 경로 목록
    const protectedPaths = ['/dashboard', '/orders', '/profile'];
    const isProtected = protectedPaths.some(p => pathname.startsWith(p));

    if (isProtected && !token) {
        const loginUrl = new URL('/login', request.url);
        loginUrl.searchParams.set('redirect', pathname);
        return NextResponse.redirect(loginUrl);
    }

    // 이미 로그인한 상태에서 로그인 페이지 접근
    if (pathname === '/login' && token) {
        return NextResponse.redirect(new URL('/dashboard', request.url));
    }

    // 모든 요청에 요청 ID 추가 (로깅/추적용)
    const response = NextResponse.next();
    response.headers.set('X-Request-Id', crypto.randomUUID());
    return response;
}

// 미들웨어 적용 경로 설정 (정적 파일 제외)
export const config = {
    matcher: ['/((?!_next/static|_next/image|favicon.ico).*)'],
};
```

---

## 6. 성능 최적화

### Image 최적화

```tsx
import Image from 'next/image';

export function ProductImage({ product }) {
    return (
        <Image
            src={product.imageUrl}
            alt={product.name}
            width={400}
            height={300}
            priority={true}           // LCP 이미지: preload 추가
            placeholder="blur"        // 로딩 중 블러 플레이스홀더
            blurDataURL={product.blurHash}
            sizes="(max-width: 768px) 100vw, 400px"  // 반응형 최적화
        />
    );
}
// next/image가 자동으로:
// - WebP/AVIF로 변환 (파일 크기 30~40% 절감)
// - 뷰포트에 맞는 크기로 리사이즈
// - lazy loading 적용 (priority 없는 경우)
// - CLS 방지 (width/height로 공간 예약)
```

### Font 최적화

```tsx
// app/layout.tsx
import { Noto_Sans_KR } from 'next/font/google';

const notoSansKr = Noto_Sans_KR({
    subsets: ['latin'],
    weight: ['400', '700'],
    display: 'swap',  // FOUT 방지
});
// 자동으로:
// - 폰트 파일 자체 호스팅 (Google Fonts 외부 요청 없음)
// - font-display: swap 적용
// - CLS 최소화

export default function RootLayout({ children }) {
    return (
        <html lang="ko" className={notoSansKr.className}>
            <body>{children}</body>
        </html>
    );
}
```

### Suspense와 Streaming

<div class="mermaid">
sequenceDiagram
    participant Browser as 브라우저
    participant Server as Next.js 서버

    Browser->>Server: GET /products
    Server-->>Browser: HTML 스트리밍 시작
    Note over Browser: 레이아웃 즉시 표시
    Server-->>Browser: ProductList 청크 완료
    Note over Browser: 상품 목록 표시
    Server-->>Browser: Recommendations 청크 완료
    Note over Browser: 추천 상품 표시
    Note over Browser: 전체 페이지 완성
